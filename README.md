# bloopi

Hardware interface to the successor to the xypi, and initially based on some of the same components and design methodologies. The main difference is that the focus is in running as a jack MIDI node, and the hardware buttons and sensors are wired directly into a Pi5.